name: Build Version 7.2 Windows Setup

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          package-manager-cache: false
      - name: Install dependencies
        run: npm install
      - name: Validate project
        run: npm run check
      - name: Build setup EXE
        run: npm run build:setup
      - name: Generate free GitHub artifact attestation
        uses: actions/attest@v4
        with:
          subject-path: 'release/installer/*.exe'

      - name: Upload setup EXE
        uses: actions/upload-artifact@v4
        with:
          name: Vintage Classic Vehicle Records Setup EXE
          path: release/installer/*.exe
          if-no-files-found: error
